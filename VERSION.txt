SYNERGOX SITE — BUILD 16
========================

LEARN PAGE — FIXED CTA + CONVERSION-FOCUSED (this build)
  The problem: the learn page's main hero button said "Start with the free
  checklist" and pointed to /#checklist — you wanted it to lead to the
  BOOK-A-CALL page. Fixed, and rebuilt the whole page to funnel toward the
  call:

    - Hero CTA now: "Book your free teardown call" -> /apply
      (reframed copy: learning the system vs knowing which part moves YOUR
       numbers first — so book the free teardown).
    - NEW mid-page conversion band (after the 4 tracks): "Not sure which
      track you actually need? Book your teardown" -> /apply.
    - Closing section CTA still -> /apply ("Book a strategy call").
    - ZERO checklist references left. Every primary (green) button on the
      page now leads to book-a-call. Secondary ghost buttons point to
      "Compare both paths" and "Run the teardown now" (supporting, not
      competing).

  So the /learn page is the do-it-yourself door, but its conversion goal —
  getting the visitor onto a free teardown call — is now consistent top to
  bottom.

EVERYTHING ELSE UNCHANGED FROM BUILD 15
  - Footer redesign (clean 3-part structure).
  - Leads fix (PROJECT_URL / SERVICE_ROLE_KEY — see FIX_LEADS.txt).
  - Paystack v2 in-page; post-payment -> /thank-you (no call refs).
  - Offer page: deliverables + comparison table + urgency/FOMO. ₦199,999.

Routes: 17. Build verified clean.
